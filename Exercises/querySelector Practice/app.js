//Select all elements that have the class of "done".
const doneTodos = document.querySelectorAll(".done");

//Select the one checkbox. Be aware of multiiple input elements.
const checkbox = document.querySelector('input[type="checkbox"]');